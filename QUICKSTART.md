# 快速开始指南

## 🎯 最快启动方式

1. **双击运行 `start.bat`**
2. 等待服务启动（约10-20秒）
3. 浏览器自动打开 `http://localhost:5173`
4. 享受明日方舟风格的博客！

## 📝 脚本说明

### start.bat - 一键启动
- 自动检查并安装npm依赖
- 在新窗口启动后端服务器（端口3001）
- 在新窗口启动前端服务器（端口5173）
- 5秒后自动打开浏览器

### stop.bat - 停止服务
- 关闭所有Node.js进程
- 停止前后端服务器

### install.bat - 仅安装依赖
- 只安装前后端的npm依赖
- 不启动服务器

## 🎮 使用流程

```
第一次使用：
  双击 start.bat → 等待安装 → 浏览器打开 → 开始使用

日常使用：
  双击 start.bat → 浏览器打开 → 开始使用

停止服务：
  双击 stop.bat 或 关闭服务器窗口
```

## ⚠️ 注意事项

1. **端口占用**：确保3001和5173端口未被占用
2. **Node.js版本**：需要Node.js 16+
3. **网络**：首次运行需要联网下载依赖

## 🐛 常见问题

**Q: 启动失败？**
A: 检查是否安装了Node.js，运行 `node -v` 确认

**Q: 浏览器没有自动打开？**
A: 手动访问 `http://localhost:5173`

**Q: 后端连接失败？**
A: 确认后端窗口没有报错，端口3001未被占用

## 🎨 功能导航

访问以下页面体验完整功能：
- **主页**: `/` - GitHub集成、技能展示
- **监控**: `/monitor` - 实时CPU/内存图表
- **铃兰**: `/operators` - 角色展示页
- **博客**: `/blog` - 文章列表
- **编辑器**: `/editor` - Markdown编辑

---

**Doctor，Rhodes Island等待你的指挥！** 🦊✨
